\documentclass[11pt,a4paper]{article}

% -------------------- Packages --------------------
\usepackage[margin=2.2cm]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{multirow}
\usepackage{graphicx}
\usepackage{float}
\usepackage{siunitx}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{amsmath}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{textcomp}

\setlist{noitemsep}

\hypersetup{
    colorlinks=true,
    linkcolor=blue!45!black,
    citecolor=blue!45!black,
    urlcolor=blue!45!black
}

\sisetup{
    round-mode=places,
    round-precision=3,
    detect-weight=true,
    detect-family=true
}

\setlist[itemize]{leftmargin=*, topsep=2pt, itemsep=2pt}
\setlist[enumerate]{leftmargin=*, topsep=2pt, itemsep=2pt}

\newcommand{\todo}[1]{\textcolor{red}{\textbf{TODO:} #1}}
\newcommand{\dataset}{Hotel Booking Demand}
\newcommand{\withadr}{R-EUCLID-robust-weighted-withADR}
\newcommand{\noadr}{R-EUCLID-robust-weighted-noADR}

% -------------------- Title --------------------
\title{\textbf{Hotel Booking Demand Clustering Study}\\
\large A Reproducible Experimental Study of Clustering}
\author{
Alexandre Santos (72970) \and
David Natal (72997) \and
Miguel Mestre (73018)
}
\date{5 June 2026\\
\small Unsupervised Learning -- 2025/2026\\
\small Repository tag: \texttt{Submission} \quad Repository: \url{https://github.com/alexandreffs/UnsupervisedLearningProj}}

\begin{document}

\maketitle

\begin{abstract}
This report presents a reproducible clustering study of hotel booking demand data
(117,430 records, course release v1) with the goal of partitioning bookings into
interpretable guest segments based solely on pre-stay observable features. Two
representations are studied: a value-inclusive pipeline (\withadr{}) and a purely
behavioural pipeline that excludes the daily rate (\noadr{}). K-Means ($k=3$,
Silhouette $=0.452$, ARI $=0.989 \pm 0.007$ across 10 seeds), iK-Means
(automatic $k=8$), and Agglomerative Hierarchical clustering (Ward, Silhouette
$=0.453$ at $k=3$) are compared under a unified evaluation protocol. The selected
three-cluster solution identifies a Non-Refundable Advance Booker segment
(12.4\%), a Standard Flexible City Guest segment (70.7\%), and a Premium Resort
Long-Stay segment (16.8\%). The noADR comparison confirms that the commitment
axis (\texttt{is\_non\_refund}) drives cluster separation independently of price.
Key limitations include the absence of ground-truth labels, the use of an ordinal
meal-plan encoding, and the fact that clustering is performed at booking level rather
than guest level.
\end{abstract}

\tableofcontents

% ============================================================
\section{Introduction}

\subsection{Context}
Hotel revenue management depends on understanding who books, when they book, and how their bookings differ in value and service requirements. Booking records contain pre-arrival signals such as rate, stay duration, booking horizon, deposit commitment, meal plan, parking need, and special requests. These signals can reveal latent booking types that support pricing, upselling, and operational planning decisions.

\subsection{Segmentation question}
The concrete segmentation question addressed in this project is:

\begin{quote}
Can hotel booking records be partitioned into distinct and interpretable behavioural segments using only pre-stay observable characteristics, such as daily rate, total estimated spend, length of stay, booking horizon, meal plan, deposit commitment, special requests, and parking need, in order to identify guest profiles that support targeted revenue-management and personalised-service strategies?
\end{quote}

The unit of analysis is the individual booking record. A guest may appear more than once in the dataset, so the clusters should be interpreted as booking segments rather than permanent guest identities.

\subsection{Why clustering is appropriate}
No ground-truth segment labels are available. The aim is therefore discovery rather than prediction, making unsupervised clustering appropriate. Clustering is also suitable because the dataset contains heterogeneous booking behaviours in lead time, price, stay duration, deposit type, and service requests. To avoid leakage, only variables available at the time of booking are used as clustering inputs; outcome and post-event variables are reserved for post-hoc interpretation only.

\subsection{Research questions}
The report addresses the course research questions as follows:
\begin{itemize}
    \item \textbf{RQ1 -- Problem framing:} define a booking-level segmentation question and justify clustering.
    \item \textbf{RQ2 -- Representation:} define leakage-safe preprocessing and feature representations for mixed-type booking data.
    \item \textbf{RQ3 -- Modelling:} compare K-Means, iK-Means, and Ward hierarchical clustering.
    \item \textbf{RQ4 -- Interpretation:} profile the discovered clusters as booking segments.
    \item \textbf{RQ5 -- Robustness:} evaluate sensitivity to seeds, subsampling, scaling, feature weighting, and representation changes.
\end{itemize}

% ============================================================
\section{Dataset and Preprocessing}

\subsection{Dataset documentation}
\begin{table}[H]
\centering
\caption{Dataset documentation and traceability.}
\begin{tabularx}{\textwidth}{lX}
\toprule
\textbf{Item} & \textbf{Details} \\
\midrule
Dataset & \dataset{} \\
Version & Course release v1 \\
Unit of analysis & One row corresponds to one booking record \\
Original size & 119,390 rows and 32 columns \\
Time span & Arrivals from July 2015 to August 2017 \\
Source & António, de Almeida, and Nunes (2019); dataset provided through the course platform \\
Raw-data governance & Raw CSV is not committed to the repository; it must be placed in \texttt{data/} according to the repository instructions \\
Checksum & SHA-256: \texttt{7c2ae42a7353905ea136e5c2287f17c92c5435826598bfbb8491c6f0c7b1fc06} \\
Usage terms & Dataset used under the course release terms; no raw data redistributed in the repository \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Data quality}
The main data-quality issues identified are:
\begin{itemize}
    \item \texttt{children} has 4 missing values, imputed as 0 because missing children counts are interpreted as no children in the booking.
    \item \texttt{country} has 488 missing values and is retained only for post-hoc profiling, not as a clustering input.
    \item \texttt{agent} and \texttt{company} have high missingness and high cardinality; both are dropped as ID-like fields.
    \item \texttt{adr} has a heavy right tail (maximum approximately \texteuro{}5,400) and one negative value, requiring robust preprocessing and careful interpretation.
    \item Rows with \texttt{adr = 0} are excluded from clustering because they correspond to complementary or test-like bookings. This removes 1,960 records, leaving 117,430 analytic records.
\end{itemize}

\begin{table}[H]
\centering
\caption{Main missingness and governance decisions.}
\begin{tabularx}{\textwidth}{lrrX}
\toprule
\textbf{Variable} & \textbf{Missing} & \textbf{Role} & \textbf{Decision} \\
\midrule
\texttt{children} & 4 & Numerical & Impute as 0 \\
\texttt{country} & 488 & Categorical & Exclude from clustering; retain for profiling \\
\texttt{agent} & 16,340 & ID-like categorical & Drop due to high missingness/cardinality \\
\texttt{company} & 112,593 & ID-like categorical & Drop due to high missingness/cardinality \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Leakage control}
The segmentation index time is the booking creation time. Therefore, only variables available at or before booking time can be used to form clusters. The following variables are excluded from clustering inputs:
\begin{itemize}
    \item \texttt{is\_canceled}: outcome variable.
    \item \texttt{reservation\_status}: post-decision/post-arrival status.
    \item \texttt{reservation\_status\_date}: date associated with the realised status.
    \item \texttt{agent} and \texttt{company}: high-cardinality ID-like fields.
    \item \texttt{country}: excluded from clustering to avoid geographical profiling effects; retained only for post-hoc descriptive analysis.
\end{itemize}

\subsection{Feature engineering}
The final clustering features are:
\begin{itemize}
    \item \texttt{total\_nights} = \texttt{stays\_in\_week\_nights} + \texttt{stays\_in\_weekend\_nights}.
    \item \texttt{total\_spend\_log} = $\log(1+\texttt{adr}) \times \texttt{total\_nights}$, used as a compressed estimated-spend proxy.
    \item \texttt{is\_non\_refund} = 1 if \texttt{deposit\_type} is Non Refund, 0 otherwise.
    \item \texttt{lead\_time\_rank}, an ordinal quantile binning of \texttt{lead\_time}: 0 for last-minute, 1 for mid-horizon, and 2 for advance bookings.
    \item \texttt{meal\_encoded}, an ordinal encoding of meal-plan inclusivity: SC/Undefined = 0, BB = 1, HB = 2, FB = 3.
    \item \texttt{total\_of\_special\_requests}.
    \item \texttt{required\_car\_parking\_spaces}.
\end{itemize}

\subsection{Representations and distance}
Two Euclidean representations are studied:
\begin{table}[H]
\centering
\caption{Final representations.}
\label{tab:representations}
\small
\begin{tabularx}{\textwidth}{p{0.32\textwidth}X}
\toprule
\textbf{Representation} & \textbf{Definition} \\
\midrule

\withadr{} &
\textbf{Features:} 
\texttt{total\_spend\_log}, 
\texttt{total\_nights}, 
\texttt{is\_non\_refund}, 
\texttt{total\_of\_special\_requests}, 
\texttt{required\_car\_parking\_spaces}, 
\texttt{meal\_encoded}, 
\texttt{lead\_time\_rank}.
\newline
\textbf{Scaling/weights:} RobustScaler on continuous variables; 
\texttt{is\_non\_refund} $\times 3.0$; 
\texttt{meal\_encoded} $\times 0.5$. \\

\addlinespace

\noadr{} &
\textbf{Features:} same as \withadr{}, excluding 
\texttt{total\_spend\_log}.
\newline
\textbf{Scaling/weights:} same scaling and weights as \withadr{}. \\

\bottomrule
\end{tabularx}
\end{table}

All models and internal validity indices are computed in the same weighted Euclidean space used to fit the corresponding clustering model.

% ============================================================
\section{Clustering Methods}

\subsection{K-Means baseline}
K-Means is used as the mandatory baseline. The predefined search interval is $k \in \{2,\ldots,10\}$, chosen because $k=2$ is the minimum meaningful segmentation and values above 10 are unlikely to remain managerially interpretable. The configuration is \texttt{n\_init=10}, \texttt{max\_iter=300}, and \texttt{random\_state=42} for the main run.

\subsection{iK-Means}
iK-Means is included as an automatic cluster-discovery and initialisation-oriented method. Mirkin's intelligent initialisation procedure extracts anomalous clusters greedily from the full 117,430-row matrix with \texttt{min\_cluster\_size=1000}. This minimum size was predefined because clusters below 1,000 bookings are unlikely to be managerially actionable. The retained number of clusters is determined automatically by the procedure; in the final run, iK-Means retained $k=8$.

\subsection{Agglomerative hierarchical clustering}
Agglomerative hierarchical clustering with Ward linkage is used as the alternative clustering family. Ward linkage is compatible with the chosen representation because the features are embedded in a Euclidean vector space. Due to computational cost, Ward clustering is run on a reproducible subsample of 30,000 records. The main subsample uses seed 42, and robustness is assessed using additional subsamples.

% ============================================================
\section{Experimental Protocol}

\subsection{Internal validity}
Models are evaluated using:
\begin{itemize}
    \item \textbf{Silhouette score} as the primary compactness/separation index.
    \item \textbf{Calinski--Harabasz index} as an additional compactness/separation index, where higher is better.
    \item \textbf{Davies--Bouldin index}, where lower is better.
\end{itemize}

\subsection{Family-specific diagnostics}
For hierarchical clustering, a Ward dendrogram is used to inspect merge structure and support the choice of the cut. For iK-Means, the automatic number of clusters and any anomalous extracted clusters should be documented.

\subsection{Stability and sensitivity}
The robustness protocol includes:
\begin{itemize}
    \item K-Means repeated across 10 random seeds for $k \in \{2,3,4,5\}$.
    \item Adjusted Rand Index (ARI) between runs as the stability measure.
    \item Hierarchical clustering repeated on controlled subsamples.
    \item RobustScaler versus StandardScaler as a preprocessing sensitivity check.
    \item withADR versus noADR comparison to test whether clusters are driven only by price.
    \item PCA representation study as Extension Module E4.
\end{itemize}

\subsection{Reproducibility}
All experiments are intended to be reproducible from the repository using:
\begin{itemize}
    \item \texttt{experiments.csv} for experiment traceability.
    \item \texttt{environment.yml} or equivalent lockfile for dependencies.
    \item \texttt{run\_all.py} as the single-entry point.
    \item \texttt{figures/} and \texttt{tables/} generated automatically from code.
\end{itemize}

% ============================================================
\section{Results}

\subsection{Data preparation outcomes}
After excluding zero-ADR records, the analytic dataset contains 117,430 bookings. The withADR matrix has 7 features and the noADR matrix has 6 features. PCA on the withADR representation indicates that the first principal component explains approximately 54.3\% of the variance and that four components are needed to reach around 90\% explained variance.

\subsection{Model comparison}
\begin{table}[H]
\centering
\caption{Main model comparison: quantitative results.}
\label{tab:model-comparison-metrics}
\small
\begin{tabular}{llrrrr}
\toprule
\textbf{Method} & \textbf{Setting} & \textbf{Sil.} & \textbf{CH} & \textbf{DB} & \textbf{Stability} \\
\midrule
K-Means & $k=3$, withADR & 0.452 & 63,837 & 0.857 & ARI $0.989 \pm 0.007$ \\
K-Means & $k=2$, withADR & 0.392 & 54,947 & 1.074 & ARI $0.998 \pm 0.002$ \\
K-Means & $k=3$, noADR & 0.490 & 48,318.07 & 0.697 & n/a \\
iK-Means & $k=8$ (auto) & 0.327 & 56,165 & 1.012 & n/a \\
Ward & $k=3$, withADR, $n=30{,}000$ & 0.453 & 15,928 & 0.832 & Sil. $\pm 0.004$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Main model comparison: interpretation.}
\label{tab:model-comparison-notes}
\small
\begin{tabularx}{\textwidth}{p{0.24\textwidth}X}
\toprule
\textbf{Model / setting} & \textbf{Observation} \\
\midrule

K-Means, $k=3$, withADR &
Selected solution. It gives the best balance between internal validity, seed stability, and business interpretability. \\

K-Means, $k=2$, withADR &
Stable but too coarse: it merges the Premium Resort Long-Stay profile with the Standard Flexible City Guest profile and loses useful granularity. \\

K-Means, $k=3$, noADR &
Useful sensitivity check. The noADR solution separates a Special Requests group, a Standard Flexible group, and a Non-Refundable high-lead-time group, confirming that the commitment axis remains visible without price. \\

iK-Means &
Automatic $k=8$ fragments the main groups into smaller clusters. It is therefore useful as a diagnostic but is not selected as the primary segmentation because it does not reveal qualitatively new booking profiles. \\

Ward, $k=3$, withADR &
Conceptually distinct method that corroborates the same three-cluster structure on a reproducible subsample. \\

\bottomrule
\end{tabularx}
\end{table}

The selected solution is K-Means with $k=3$ on the withADR representation. This choice is justified because it achieves the strongest balance between internal validity, stability, and interpretability. Although $k=2$ is stable, it merges two managerially different segments. Values above $k=3$ tend to fragment the same business profiles without producing clearer narratives. iK-Means is not selected as the primary solution because its automatic $k=8$ solution fragments the three main groups without revealing qualitatively new profiles.

\subsection{Cluster profiles}
\begin{table}[H]
\centering
\caption{Selected K-Means cluster profiles for $k=3$ withADR.}
\begin{tabularx}{\textwidth}{lrrXl}
\toprule
\textbf{Cluster} & \textbf{Size} & \textbf{\%} & \textbf{Main characteristics} & \textbf{Label} \\
\midrule
C2 & 14,513 & 12.4 & ADR approximately 	exteuro{}90, 	exttt{is	extunderscore non	extunderscore refund}$=1.0$, stay 2.7 nights, advance lead-time profile & Non-Refundable Advance Booker \\
C1 & 83,139 & 70.7 & ADR approximately 	exteuro{}103, flexible deposit, stay 2.6 nights, last-minute/mid lead-time profile, highest parking need & Standard Flexible City Guest \\
C0 & 19,778 & 16.8 & ADR approximately 	exteuro{}114, stay 7.7 nights, total spend 136\% above the mean, most special requests, resort-hotel dominant & Premium Resort Long-Stay \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Cluster interpretation}
The \textbf{Non-Refundable Advance Booker} segment is defined by full deposit commitment (\texttt{is\_non\_refund}$=1.0$ for all members), a more advanced booking horizon, and short stays of about 2.7 nights. Although it has the lowest ADR among the three main clusters, it provides high revenue certainty because cancellation flexibility is limited.

The \textbf{Standard Flexible City Guest} segment forms the majority of bookings (70.7\%). These records are typically flexible, short-stay bookings with no non-refundable commitment, an average stay of about 2.6 nights, and the highest parking-need proportion. This segment likely contains both business and urban leisure demand.

The \textbf{Premium Resort Long-Stay} segment is the smallest but most valuable profile. It is characterised by longer stays of about 7.7 nights, ADR around \texteuro{}114, total estimated spend 136\% above the grand mean, and more special requests. This segment is suitable for pre-arrival personalisation and upselling.

\subsection{noADR comparison}
The noADR representation tests whether clusters are merely price bands. Removing \texttt{total\_spend\_log} reduces the explicit price/value signal. The Non-Refundable segment remains structurally meaningful, indicating that deposit commitment is an independent axis of segmentation. However, separation between Standard and Premium bookings becomes weaker because price and total spend are important for distinguishing high-value long-stay behaviour.

\subsection{PCA representation study: Extension Module E4}
\begin{table}[H]
\centering
\caption{PCA representation study for K-Means with $k=3$.}
\label{tab:pca}
\begin{tabular}{lrrrr}
\toprule
\textbf{Representation} & \textbf{Variance explained} & \textbf{Silhouette} & \textbf{CH} & \textbf{ARI vs original} \\
\midrule
Original 7D & 1.000 & 0.452 & 63,837 & 1.000 \\
PCA-2 & 0.354 & 0.506 & 56,008.27 & 0.609 \\
PCA-3 & 0.503 & 0.436 & 48,439.12 & 0.670 \\
PCA-4 & 0.638 & 0.458 & 44,781.98 & 0.964 \\
\bottomrule
\end{tabular}
\end{table}

PCA-4 retains 63.8\% of the variance and reaches an ARI of 0.964 against the original solution, showing that it recovers the original three-cluster structure almost perfectly, although it does not improve interpretability.

The main methodological caveat is that preserving variance is not equivalent to preserving cluster-separating directions. In this dataset, the deposit-commitment feature has relatively low marginal variance but strong segmentation meaning. PCA may therefore compress behaviourally important dimensions into later components. The original 7D representation is retained unless PCA produces comparable ARI and internal validity without reducing interpretability.

% ============================================================
\section{Discussion}

\subsection{Which result best answers the segmentation question?}
K-Means with $k=3$ on the withADR representation best answers the segmentation question. It provides three stable and interpretable booking profiles that align with actionable revenue-management decisions: commitment-based rate locking, flexible city-hotel demand management, and high-value resort-stay personalisation.

\subsection{Influence of preprocessing}
The feature weight on \texttt{is\_non\_refund} is one of the most consequential preprocessing choices. Without weighting, the binary commitment signal is dominated by continuous features. The meal-plan weight reduces the effect of an ordinal encoding that might otherwise exaggerate differences between meal categories. These weights make the Euclidean metric more aligned with the business segmentation question, but they also introduce subjective assumptions.

\subsection{Stability and sensitivity}
K-Means is highly stable across seeds for the selected $k=3$ solution. Hierarchical Ward clustering provides independent support for the three-cluster structure. Sensitivity checks using scaler changes and the noADR representation indicate that the main qualitative conclusions are not driven solely by a single preprocessing decision, although the distinction between Standard and Premium segments depends partly on value-related features.

\subsection{Decision-relevant recommendations}
\begin{itemize}
    \item \textbf{Non-Refundable Advance Booker:} offer early-bird packages and prepaid upgrades at booking time.
    \item \textbf{Standard Flexible City Guest:} use targeted flexibility-based promotions, parking-inclusive packages, and last-minute offers.
    \item \textbf{Premium Resort Long-Stay:} prioritise pre-arrival personalisation, room upgrades, spa/F\&B offers, and long-stay service planning.
\end{itemize}

\subsection{What should not be concluded}
The clusters are not externally validated customer types. They are booking-level geometric segments, not permanent guest identities. A returning guest may appear in different clusters across different stays. Internal validity indices measure geometric compactness and separation, not business profitability or causal effects.

% ============================================================
\section{Limitations, Ethics, and Reproducibility}

\subsection{Limitations}
\begin{itemize}
    \item There are no ground-truth segment labels.
    \item The unit of analysis is a booking record, not a unique guest.
    \item The meal-plan encoding assumes an ordinal structure and equal spacing between meal levels.
    \item Lead-time quantile binning simplifies a highly skewed continuous variable.
    \item Manual feature weights encode subjective modelling assumptions.
    \item The dataset concerns two Portuguese hotels from 2015--2017, limiting generalisability.
    \item iK-Means produced an automatic $k=8$ solution, but this result should be treated as exploratory because it fragments the main segments and was more computationally demanding than the selected K-Means baseline.
\end{itemize}

\subsection{Ethics and governance}
The dataset is used according to the course release terms. Raw data are not committed to the repository. No direct personal identifiers are used. Country is excluded from clustering inputs and retained only, if needed, for post-hoc descriptive analysis.

\subsection{Reproducibility checklist}
The final repository should contain:
\begin{itemize}
    \item \texttt{README.md} with setup and execution instructions.
    \item \texttt{environment.yml} or equivalent lockfile.
    \item \texttt{run\_all.py} as a single-entry point.
    \item \texttt{experiments.csv} with method, parameters, seeds, metrics, diagnostics, and notes.
    \item \texttt{src/}, \texttt{figures/}, and \texttt{tables/} folders.
    \item A \texttt{Submission} tag or GitHub release.
\end{itemize}

\subsection{Academic integrity disclosure}
Generative-AI tools were consulted only for methodological discussion, syntax review, and report-structure support. All final analytical decisions, modelling choices, experimental results, and interpretations must be validated by the team.

% ============================================================
\section{Conclusion}
This study segments 117,430 hotel booking records using leakage-safe pre-stay features. The selected K-Means solution with $k=3$ on the withADR representation identifies three interpretable booking profiles: Non-Refundable Advance Booker, Standard Flexible City Guest, and Premium Resort Long-Stay. The results are supported by internal validation, seed stability, and comparison with Ward hierarchical clustering. The noADR comparison shows that booking commitment remains an important segmentation axis independently of price, while the value-inclusive representation better answers the revenue-management question. Future work should validate the segments against business outcomes, test alternative encodings, and evaluate generalisability across other hotels and time periods.

% ============================================================
\appendix

\section{Feature Table}
\begin{table}[H]
\centering
\caption{withADR feature table.}
\begin{tabular}{llll}
\toprule
\textbf{Feature} & \textbf{Type} & \textbf{Scaling} & \textbf{Weight} \\
\midrule
\texttt{total\_spend\_log} & Continuous & RobustScaler & $\times 1.0$ \\
\texttt{total\_nights} & Continuous & RobustScaler & $\times 1.0$ \\
\texttt{is\_non\_refund} & Binary & Raw & $\times 3.0$ \\
\texttt{total\_of\_special\_requests} & Count & Raw & $\times 1.0$ \\
\texttt{required\_car\_parking\_spaces} & Binary/count & Raw & $\times 1.0$ \\
\texttt{meal\_encoded} & Ordinal & Raw & $\times 0.5$ \\
\texttt{lead\_time\_rank} & Ordinal & Raw & $\times 1.0$ \\
\bottomrule
\end{tabular}
\end{table}

\section{K-Means Stability Summary}
\begin{table}[H]
\centering
\caption{K-Means stability across 10 seeds.}
\begin{tabular}{rrrrrr}
\toprule
\textbf{k} & \textbf{Mean ARI} & \textbf{Std ARI} & \textbf{Mean Sil.} & \textbf{Std Sil.} & \textbf{Notes} \\
\midrule
2 & 0.9975 & 0.0021 & --- & --- & Stable but under-segmented \\
3 & 0.989 & 0.007 & 0.452 & 0.000 & Selected solution \\
4 & 0.9942 & 0.0057 & --- & --- & Additional fragmentation \\
5 & 0.9803 & 0.0138 & --- & --- & Less interpretable \\
\bottomrule
\end{tabular}
\end{table}

\section{Scaler Sensitivity}
\begin{table}[H]
\centering
\caption{Scaler sensitivity for numerical features, $k=3$.}
\begin{tabular}{lrrr}
\toprule
\textbf{Scaler} & \textbf{Silhouette} & \textbf{CH} & \textbf{DB} \\
\midrule
RobustScaler & 0.452 & 63,837 & 0.857 \\
StandardScaler & 0.3855 & 36,195.42 & 0.9812 \\
\bottomrule
\end{tabular}
\end{table}

\begin{thebibliography}{9}

\bibitem{antonio2019}
N. António, A. de Almeida, and L. Nunes.
\newblock Hotel booking demand datasets.
\newblock \emph{Data in Brief}, 22:41--49, 2019.
\newblock \url{https://doi.org/10.1016/j.dib.2018.11.126}

\bibitem{mirkin2005}
B. Mirkin.
\newblock \emph{Clustering for Data Mining: A Data Recovery Approach}.
\newblock Chapman \& Hall/CRC, 2005.

\bibitem{kaufman1990}
L. Kaufman and P. J. Rousseeuw.
\newblock \emph{Finding Groups in Data: An Introduction to Cluster Analysis}.
\newblock Wiley, 1990.

\bibitem{pedregosa2011}
F. Pedregosa et al.
\newblock Scikit-learn: Machine learning in Python.
\newblock \emph{Journal of Machine Learning Research}, 12:2825--2830, 2011.

\end{thebibliography}

\end{document}
